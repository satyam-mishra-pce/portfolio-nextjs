# Agent notes

## Content style

In user facing copy (e.g. `src/lib/data.ts`), never use hyphens, em dashes, en dashes, or colons unless asked. They read as AI written. Use commas, periods, and separate sentences instead. Write "full stack", "end to end", "onchain", not "full-stack", "end-to-end", "on-chain".
